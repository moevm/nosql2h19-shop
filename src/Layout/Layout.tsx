import React from 'react';
import { Header, Body } from "./index";

const Layout: React.FC = () => {
    return (
        <div>
            <Header />
            <Body />
        </div>
    );
}

export default Layout;